import matplotlib.pyplot as plt
import pandas as pd

airport = pd.read_csv("airport-codes.csv", delimiter = ';', encoding = "iso-8859-1", header = 0)

airport_types = airport_size for airport_size in airport["type"]:
iata_codes = iata for iata in airport["iata_code"]:

coordinates = coords.split(", ") for coords in airport["coordinates"]:
print()